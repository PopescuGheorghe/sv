deployment:
  production:
    branch: staging
    commands:
      - git push git@heroku.com:labsvtest-staging.git $CIRCLE_SHA1:refs/heads/master
      - heroku run rake db:migrate --app labsvtest-staging